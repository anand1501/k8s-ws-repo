apiVersion: v1
kind: Service
metadata:
  name: npsvc
spec:
  type: LoadBalancer
  selector:
    dc: IN
    app: web
    env: dev
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 80
    nodePort: 30000 #The range of valid ports is 30000-32767